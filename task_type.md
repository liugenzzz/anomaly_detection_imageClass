# 两级异常检测图片分类规则

可执行提示词以 `pipeline/stages/task_type/prompts.py` 为准。分类只依据图片像素，不使用文件名、路径名、目录名或图片外背景信息。

## 一级分类

一级分类用于从原始数据集中筛选任务领域：

1. `军事异常检测`
2. `工业设备异常检测`
3. `飞机螺丝及结构异常检测`
4. `其它异常`

必须同时存在目标场景和目标异常的可见证据，才可进入前三个领域。场景不符、异常不符、图片正常、证据不足或无法判断时，一级分类为 `其它异常`。

## 二级分类

只有一级分类被全部模型一致判为前三个目标领域之一时，才执行二级分类。

### 军事异常检测

- `集结`：多名人员、多辆车辆或多件装备形成明显集中、编队、队列或有组织聚集。
- `爆炸`：可见火球、冲击扬尘、碎片飞散、爆炸中心或局部破坏等直接或组合证据。
- `烟雾`：从地面、车辆、建筑、装备或设施附近产生并扩散的柱状、团状或羽流状烟雾。
- `越界移动`：存在可辨认边界，并且对象已经跨越，或具有明确跨越动作/轨迹证据。
- `其它任务类型`：不属于以上四类或证据不足。

### 工业设备异常检测

- `发热`：热成像中存在显著局部高温区，或有灼热、烧红、焦化、热致变色等直接证据。
- `泄漏`：介质从管道、阀门、接口、容器或设备破损处逸出、滴落、喷射、流淌或异常积聚。
- `结构变形`：设备或承载结构明显弯曲、扭曲、翘曲、鼓包、凹陷、错位或塌陷。
- `其它任务类型`：不属于以上三类或证据不足。

### 飞机螺丝及结构异常检测

- `飞机螺丝缺失`：飞机结构上应安装紧固件的位置出现空孔、裸露孔位，或规则紧固件序列出现明确断点。
- `飞机锈蚀`：飞机金属表面、螺丝头、孔边、接缝或结构件出现锈斑、点蚀、起皮或腐蚀产物。
- `其它任务类型`：不属于以上两类或证据不足。

一级已经判为 `其它异常` 时，跳过模型二级调用并直接保存到一级 `其它异常` 目录。一级判为前三个目标领域之一时，只把该领域对应的候选标签发送给模型执行二级分类，不允许跨领域选择二级标签。

## 全模型一致投票

一级和二级采用相同规则：

1. 将同一图片及同一级提示词并行发送给全部已选且启用的模型；访问失败、超时、被禁用或响应无法解析的模型不参与本次投票。
2. 每个模型为该级全部候选标签打分，并输出一个最高分标签。
3. 每个有效模型一票，不使用模型权重改变投票结果；各标签分数取所有有效模型的等权平均，仅用于审计。
4. 默认至少需要 `min_valid_provider_count=2` 个有效模型。一级使用 `level1_score_threshold`，二级使用 `level2_score_threshold`；所有有效模型的最高分标签完全相同，并且每个有效模型对该标签的分数都严格大于当前级阈值，才接受目标标签。
5. 任一有效模型对一致目标标签的分数未严格超过当前级阈值，一级归为 `其它异常`，二级归为当前一级领域下的 `其它任务类型`。兜底标签本身不要求其分数超过阈值。
6. 有效模型之间出现标签分歧时执行分级兜底。无有效模型或有效模型少于最小数量时同样兜底，但访问失败的模型本身不再否决其它有效模型的一致结果。

有效模型一致选择目标标签且通过阈值时标记为 `auto_accept`；有效模型一致选择当前级兜底标签时也直接接受。由阈值不足、分歧或有效模型数量不足转入兜底标签的结果标记为 `needs_review`，并在 `decision_reason` 中记录发生于一级还是二级。

## 结果输出

分类阶段只流式输出 `results.csv`，不再生成体积较大的 `results.jsonl`，模型原始长响应默认也不保存。CSV 主要字段包括：

- `classification_schema_version`：当前为 `two_level_available_provider_vote_csv_v1`。
- `best_task_type`、`best_anomaly_type`：一级和二级标签。
- `level1_score_threshold`、`level2_score_threshold`：本条结果实际使用的两级独立阈值。
- `level1_threshold_passed`、`level2_threshold_passed`：两级阈值是否通过。
- `level1_provider_votes_json`、`level2_provider_votes_json`：压缩后的逐模型投票。
- `level1_provider_scores_json`、`level2_provider_scores_json`：压缩后的逐模型投票标签分数。
- 四个一级分数列及全部二级分数列。

## 目录整理

目标领域按两级目录整理：

```text
data_by_task/
  军事异常检测/
    集结/
    爆炸/
    烟雾/
    越界移动/
    其它任务类型/
  工业设备异常检测/
    发热/
    泄漏/
    结构变形/
    其它任务类型/
  飞机螺丝及结构异常检测/
    飞机螺丝缺失/
    飞机锈蚀/
    其它任务类型/
  其它异常/
  _未分类/
```

一级 `其它异常` 和各目标领域内的二级 `其它任务类型` 都是有效分类标签；`_未分类` 仅用于结果缺失或标签无效等流程异常。
